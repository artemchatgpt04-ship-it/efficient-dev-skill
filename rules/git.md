# Git rules

- Inspect branch, status, diff, and remotes before making repository mutations.
- Preserve unrelated user changes and stage only reviewed in-scope files.
- Never rewrite history, delete data, publish, or change repository visibility without explicit authority.
- Verify status and branch synchronization after requested commits or pushes.
