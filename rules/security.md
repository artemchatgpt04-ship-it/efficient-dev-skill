# Security rules

- Treat authentication, authorization, credentials, secrets, permissions, cryptography, and sensitive data handling as high-risk scope.
- Never print, persist, or commit secrets; use configured credential mechanisms.
- Check trust boundaries, validation, least privilege, error disclosure, and secure defaults where relevant.
- Expand investigation and validation when the security impact or caller set is uncertain.
